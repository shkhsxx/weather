# PRD — World Weather Atlas (가칭)

> 세계 랜드마크를 지도에서 클릭하면 그곳의 현재 날씨와 현지 시간이 보이고,
> 날씨·시간에 따라 화면 배경 전체가 함께 변하는 웹 서비스

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 (Draft) |
| 작성일 | 2026-08-18 |
| 작성자 | 혜수 (Teamsparta) |
| 상태 | 리뷰 대기 |
| 관련 프로젝트 | 서비스기획 |

---

## 1. 배경 & 문제 정의

기존 날씨 서비스는 "내가 있는 곳의 날씨를 정확히 알려주는" 실용 도구에 최적화되어 있다.
반대로 **"지금 지구 반대편은 어떤 하늘일까"** 라는 감각적·탐색적 수요를 채워주는 서비스는 드물다.

- 여행 계획 단계에서 사람들은 "지금 그곳 분위기"를 알고 싶어 하지만, 숫자 나열형 UI는 이를 전달하지 못한다.
- 해외 협업·유학·이민 등으로 **타 지역 현지 시간**을 함께 확인해야 하는 니즈가 상시 존재한다.
- 날씨 데이터는 본질적으로 시각적인데, 대부분의 서비스가 이를 아이콘 하나로만 표현한다.

**핵심 가설**
> 날씨와 현지 시간을 "배경 전체"로 표현하면, 사용자는 데이터를 읽는 대신 **체감**하게 되고
> 이는 체류시간·재방문·공유로 이어진다.

---

## 2. 목표 (Goals)

| # | 목표 | 측정 지표 |
|---|---|---|
| G1 | 랜드마크 클릭 → 그곳의 날씨/시간을 즉각 체감시킨다 | 상세 진입 후 평균 체류 20초 이상 |
| G2 | 배경 전환 연출을 서비스의 시그니처로 만든다 | 세션당 평균 랜드마크 조회 3개 이상 |
| G3 | 검색·필터로 "원하는 하늘"을 탐색 가능하게 한다 | 세션 중 검색 또는 필터 사용률 30% 이상 |
| G4 | 첫 진입 3초 이내에 의미 있는 화면을 보여준다 | LCP 2.5초 이하 (모바일 4G 기준) |

### 비목표 (Non-Goals) — 이번 버전에서 하지 않는 것

- 회원가입 / 로그인 / 즐겨찾기 서버 동기화 (v1.1 이후)
- 기상 예보의 정확도 경쟁 (Open-Meteo 원본 데이터를 그대로 신뢰)
- 미세먼지·자외선·해양 등 부가 지표 (백로그)
- 네이티브 앱 (반응형 웹으로 대응)
- 다국어 (한국어 우선, 영어는 v1.1)

---

## 3. 타겟 유저 & 유저 스토리

### 3.1 페르소나

| 페르소나 | 설명 | 핵심 니즈 |
|---|---|---|
| **P1. 여행 준비자** (25~35세) | 3개월 내 해외여행을 계획 중 | "지금 파리 날씨 어때? 옷은 뭘 챙기지?" |
| **P2. 랜선 여행자** (20~40세) | 특별한 목적 없이 둘러보는 사용자 | "지금 지구 어딘가는 눈이 오고 있겠지" |
| **P3. 원격 협업자** (28~45세) | 해외 팀·클라이언트와 일함 | "샌프란시스코 지금 몇 시야?" |

### 3.2 유저 스토리

| ID | 스토리 | 우선순위 |
|---|---|---|
| US-01 | 사용자로서, 세계지도에서 랜드마크 핀을 클릭해 그곳의 현재 날씨를 보고 싶다 | P0 |
| US-02 | 사용자로서, 랜드마크의 **현지 시간**을 초 단위로 흐르는 시계로 보고 싶다 | P0 |
| US-03 | 사용자로서, 선택한 랜드마크의 날씨·시간에 맞춰 배경이 바뀌는 걸 보고 싶다 | P0 |
| US-04 | 사용자로서, 원하는 도시 이름을 검색해 바로 이동하고 싶다 | P0 |
| US-05 | 사용자로서, "지금 눈 오는 곳"만 걸러 보고 싶다 | P1 |
| US-06 | 사용자로서, "지금 밤인 곳"만 걸러 보고 싶다 | P1 |
| US-07 | 사용자로서, 전체 랜드마크를 리스트로 한눈에 비교하고 싶다 | P1 |
| US-08 | 사용자로서, 데이터를 불러오는 중임을 명확히 알고 싶다 | P0 |
| US-09 | 사용자로서, 오류가 났을 때 무엇이 잘못됐고 어떻게 해야 하는지 알고 싶다 | P0 |
| US-10 | 사용자로서, 현재 보고 있는 랜드마크의 3일 예보를 함께 보고 싶다 | P1 |
| US-11 | 사용자로서, 모바일에서도 동일한 경험을 하고 싶다 | P0 |

---

## 4. 핵심 컨셉 — "배경이 곧 날씨다"

화면 배경은 **2개 레이어의 조합**으로 구성한다. 조합식으로 관리하면 애셋 11개로 28가지 상태를 표현할 수 있다.

```
[Layer 1] Time Base   : 현지 시간대 기반 그라디언트 (4종)
[Layer 2] Weather FX  : 날씨 상태 기반 오버레이 (7종)
────────────────────────────────────────────────
최종 배경 = Time Base × Weather FX
```

### 4.1 Layer 1 — Time Base (현지 시간 기준)

일출/일몰 시각은 Open-Meteo `daily.sunrise` / `daily.sunset` 값을 사용해 계산한다.

| 밴드 | 조건 | 그라디언트 방향성 |
|---|---|---|
| `dawn` (새벽/일출) | sunrise −60분 ~ sunrise +60분 | 짙은 남색 → 살구빛 |
| `day` (낮) | sunrise +60분 ~ sunset −60분 | 하늘색 → 밝은 하늘 |
| `dusk` (노을/일몰) | sunset −60분 ~ sunset +60분 | 주황 → 보라 |
| `night` (밤) | 그 외 | 진남색 → 검정 |

> **Fallback**: `daily.sunrise/sunset`을 가져오지 못한 경우 `current.is_day` (1/0)로 `day`/`night`만 판정한다.

### 4.2 Layer 2 — Weather FX (WMO Weather Code 기준)

| FX 그룹 | WMO 코드 | 연출 |
|---|---|---|
| `clear` | 0, 1 | 오버레이 없음. 낮이면 렌즈 플레어, 밤이면 별 파티클 |
| `cloudy` | 2, 3 | 반투명 구름 레이어 느린 수평 이동 |
| `fog` | 45, 48 | 전체 블러 + 흰 안개 그라디언트, 랜드마크 실루엣 흐림 |
| `rain` | 51·53·55, 56·57, 61·63·65, 66·67, 80·81·82 | 빗줄기 파티클 (강도에 따라 밀도 3단계) |
| `snow` | 71·73·75, 77, 85·86 | 눈 파티클 (강도 3단계), 배경 채도 다운 |
| `thunder` | 95, 96, 99 | 어두운 배경 + 3~7초 랜덤 간격 번쩍임 |
| `unknown` | 위 매핑 외 | `cloudy`로 폴백 |

### 4.3 전환 규칙

- 랜드마크 전환 시 배경은 **600ms cross-fade**. 파티클은 새 상태가 페이드인되며 기존 파티클은 소멸.
- 배경 전환 중에도 지도/패널 인터랙션은 차단하지 않는다.
- **접근성**: `prefers-reduced-motion: reduce` 감지 시 파티클·번쩍임·구름 이동을 모두 정지하고 정적 그라디언트만 적용한다. (필수 요구사항)
- 파티클 렌더는 Canvas 1개로 통합. 탭이 백그라운드일 때(`visibilitychange`) 렌더 루프 정지.

---

## 5. 정보 구조 (IA)

```
/  (Home — 단일 페이지)
├── 배경 레이어 (전역, 선택된 랜드마크 상태를 반영)
├── 상단 바
│   ├── 로고 / 서비스명
│   ├── 검색창 (도시·랜드마크 검색)
│   └── 뷰 전환 토글 [지도 | 리스트]
├── 필터 바 (지도·리스트 뷰 공통 적용)
│   ├── 대륙/지역
│   ├── 현재 날씨 상태
│   ├── 현지 시간대 (낮/밤)
│   └── 기온 범위
├── 메인 영역
│   ├── [A] 지도 뷰 — 인터랙티브 세계지도 + 랜드마크 핀
│   └── [B] 리스트 뷰 — 랜드마크 카드 목록
├── 상세 패널 (랜드마크 선택 시)
└── 푸터 — Open-Meteo 출처 표기 (CC BY 4.0, 필수)
```

**상태 관리**: 선택된 랜드마크 ID, 뷰 모드, 필터 조건을 URL 쿼리스트링에 반영한다.
예) `/?view=map&spot=eiffel-tower&region=europe&sky=snow`
→ 공유·새로고침 시 동일 상태 복원. **공유 유입이 핵심 성장 경로이므로 P0.**

---

## 6. 기능 요구사항

### F-01. 인터랙티브 세계지도 (P0)

| ID | 요구사항 |
|---|---|
| F-01-1 | 세계 전도를 표시하고, 큐레이션된 랜드마크 위치에 핀을 렌더링한다. |
| F-01-2 | 핀에는 해당 지역의 **현재 날씨 아이콘**과 **기온**이 뱃지 형태로 함께 표시된다. |
| F-01-3 | 핀 hover 시 랜드마크명 + 현지시간 툴팁을 노출한다. (모바일은 hover 없음 → 탭 = 선택) |
| F-01-4 | 핀 클릭 시 상세 패널이 열리고, 지도는 해당 핀 중심으로 부드럽게 이동한다. |
| F-01-5 | 확대/축소(휠, 핀치, 버튼), 드래그 이동을 지원한다. 줌 레벨은 2~8로 제한. |
| F-01-6 | 필터 적용 시 조건에 맞지 않는 핀은 지도에서 제거(또는 20% 투명도)된다. |
| F-01-7 | 줌 아웃 상태에서 핀이 겹칠 경우 클러스터링하여 개수를 표시한다. |
| F-01-8 | 지도 타일은 배경 연출을 가리지 않도록 **다크/미니멀 스타일**을 사용하고, 선택된 랜드마크의 시간대(`day`/`night`)에 따라 지도 테마도 함께 전환한다. |

> **기술 결정 필요**: 지도 라이브러리 — Leaflet + 무료 타일(CARTO) / MapLibre GL / d3-geo 기반 SVG 자체 렌더.
> 랜드마크가 26개로 고정이고 상세 지리정보가 불필요하므로 **d3-geo SVG 자체 렌더가 유력** (외부 타일 의존·비용 없음, 배경과 톤 일치 용이). → 오픈 이슈 OI-1

### F-02. 랜드마크 상세 패널 (P0)

핀 클릭 또는 리스트 카드 클릭 시 노출. 데스크톱은 우측 사이드 패널, 모바일은 하단 바텀시트.

| 영역 | 표시 항목 |
|---|---|
| 헤더 | 랜드마크명 / 도시 · 국가 / 국기 |
| **현지 시간** | `HH:MM:SS` **초 단위 실시간 갱신** + 요일·날짜 + 한국 시간 대비 시차 (예: `KST −7h`) |
| 현재 날씨 | 대형 날씨 아이콘, 현재 기온, 체감온도, 날씨 상태 텍스트(한국어) |
| 상세 지표 | 습도(%), 강수량(mm), 풍속(m/s), 구름량(%) |
| 일출·일몰 | 오늘의 일출/일몰 시각 (현지 기준) |
| 3일 예보 | 날짜별 아이콘 / 최고·최저 기온 (US-10, P1) |
| 액션 | 닫기 / 링크 복사 |

- **F-02-1** 현지 시간은 클라이언트 타이머로 1초마다 갱신한다. 기준값은 응답의 `utc_offset_seconds`를 사용하며, 사용자 기기 시각이 아닌 **서버 응답 시각 + 경과 시간**으로 계산해 기기 시계 오차의 영향을 줄인다.
- **F-02-2** 패널이 열리는 즉시 배경 전환이 시작된다. (F-04 규칙 적용)
- **F-02-3** ESC 키 또는 패널 밖 클릭으로 닫힌다. 닫으면 배경은 직전 상태 또는 기본 배경으로 복귀한다.

### F-03. 도시 검색창 (P0)

| ID | 요구사항 |
|---|---|
| F-03-1 | 상단 바에 상시 노출. 플레이스홀더: `도시나 랜드마크를 검색해 보세요` |
| F-03-2 | **2글자 이상** 입력 시 자동완성 드롭다운을 노출한다. (Geocoding API 제약: 2자 미만은 결과 없음) |
| F-03-3 | 입력 후 **300ms 디바운스**를 적용해 API 호출량을 제어한다. |
| F-03-4 | 검색 결과는 ① 큐레이션 랜드마크 우선 매칭 → ② Open-Meteo Geocoding API 결과 순으로 정렬해 노출한다. |
| F-03-5 | 결과 항목에는 도시명 / 행정구역(`admin1`) / 국가(`country`)를 함께 표시해 동명 도시를 구분한다. |
| F-03-6 | 결과 선택 시 해당 좌표의 날씨를 조회해 상세 패널을 열고, 지도를 이동한다. (랜드마크가 아닌 임의 도시도 동일 UI로 표시) |
| F-03-7 | 결과 0건 시 "검색 결과가 없어요. 다른 이름으로 시도해 보세요" 빈 상태를 노출한다. |
| F-03-8 | 키보드 ↑↓ 이동, Enter 선택, ESC 닫기를 지원한다. |
| F-03-9 | 최근 검색 5건을 localStorage에 저장해 포커스 시 노출한다. (P2) |

### F-04. 필터 (P1)

4개 필터는 **AND 조건**으로 동시 적용되며, 지도 뷰와 리스트 뷰에 동일하게 반영된다.

| 필터 | 타입 | 옵션 |
|---|---|---|
| **대륙/지역** | 다중 선택 칩 | 아시아 / 유럽 / 북미 / 남미 / 아프리카 / 오세아니아 |
| **현재 날씨** | 다중 선택 칩 | 맑음 / 구름 / 안개 / 비 / 눈 / 뇌우 (§4.2 FX 그룹과 동일 기준) |
| **현지 시간대** | 단일 선택 토글 | 전체 / ☀️ 지금 낮 / 🌙 지금 밤 (`current.is_day` 기준) |
| **기온 범위** | 듀얼 핸들 슬라이더 | −40℃ ~ 50℃, 1℃ 단위 |

- **F-04-1** 필터 적용 결과가 0건이면 "조건에 맞는 곳이 없어요. 필터를 조정해 보세요" + `필터 초기화` 버튼을 노출한다.
- **F-04-2** 적용된 필터 개수를 필터 바에 뱃지로 표시하고, `전체 초기화`를 제공한다.
- **F-04-3** 필터 상태는 URL 쿼리스트링에 반영된다.
- **F-04-4** 필터는 **이미 로드된 데이터에 대한 클라이언트 사이드 연산**이다. 필터 변경 시 추가 API 호출을 하지 않는다.
- **F-04-5** 모바일에서는 필터 바를 바텀시트 모달로 대체하고, `적용` 버튼을 눌러야 반영한다.

### F-05. 리스트 뷰 (P1)

| ID | 요구사항 |
|---|---|
| F-05-1 | 상단 토글로 지도 ↔ 리스트를 전환한다. 필터·검색 상태는 전환 시 유지된다. |
| F-05-2 | 랜드마크당 카드 1개. 카드에는 랜드마크명, 도시·국가, 날씨 아이콘, 현재 기온, 현지 시간(분 단위), 날씨 상태 텍스트를 표시한다. |
| F-05-3 | 카드 배경은 해당 지역의 Time Base 그라디언트를 **축소 적용**해 리스트 자체가 지구의 낮/밤 띠처럼 보이게 한다. |
| F-05-4 | 정렬을 지원한다: 기온 높은 순 / 낮은 순 / 현지 시간 빠른 순 / 이름순 / 기본(대륙별) |
| F-05-5 | 카드 클릭 시 상세 패널이 열리고 배경이 전환된다. |
| F-05-6 | 그리드는 데스크톱 3열 / 태블릿 2열 / 모바일 1열로 반응한다. |

### F-06. 로딩 화면 (P0)

로딩은 **3단계**로 구분해 각각 다른 UI를 사용한다. "전체를 가리는 스피너"는 초기 진입에만 허용한다.

| 단계 | 상황 | UI |
|---|---|---|
| **L1. 초기 진입** | 앱 부트 + 전체 랜드마크 날씨 일괄 조회 | 풀스크린 스플래시. 밤→낮으로 흐르는 그라디언트 애니메이션 + 로고 + `전 세계의 하늘을 불러오는 중…` + 진행률 바 |
| **L2. 부분 갱신** | 개별 랜드마크 상세 조회, 검색 결과 조회 | 해당 영역에만 **스켈레톤 UI**. 화면 전체는 그대로 인터랙션 가능 |
| **L3. 백그라운드 갱신** | 10분 주기 자동 리프레시 | 상단에 얇은 프로그레스 라인만 표시. 사용자 흐름을 막지 않음 |

- **F-06-1** L1이 **8초를 초과**하면 "연결이 느린 것 같아요" 안내와 `다시 시도` 버튼을 스플래시 내에 노출한다.
- **F-06-2** L1 동안에도 지도 윤곽(정적 SVG)은 먼저 렌더해 체감 대기 시간을 줄인다.
- **F-06-3** 모든 로딩 요소에 `aria-busy="true"` 및 `role="status"`를 부여한다.
- **F-06-4** 스켈레톤 애니메이션도 `prefers-reduced-motion`을 따른다.

### F-07. 에러 화면 (P0)

에러는 **원인별로 다른 화면과 다른 복구 액션**을 제공한다. 공통 문구 하나로 뭉개지 않는다.

| 코드 | 상황 | 화면 | 복구 액션 |
|---|---|---|---|
| **E-01** | 네트워크 연결 없음 (offline) | 풀스크린. `인터넷 연결이 끊어졌어요` | `다시 시도` + 온라인 복귀 시 자동 재시도 |
| **E-02** | API 5xx / 타임아웃(10초) | 풀스크린 또는 영역 단위. `날씨 정보를 불러오지 못했어요` | `다시 시도` (지수 백오프 재시도 3회 후 노출) |
| **E-03** | Rate limit 초과 (429) | 배너. `요청이 많아 잠시 후 다시 불러올게요` | 60초 후 자동 재시도, 그동안 **캐시 데이터를 계속 표시** |
| **E-04** | 일부 랜드마크만 조회 실패 | 해당 핀/카드에만 `—` 표시 + 오류 아이콘 | 카드 개별 `재시도` 버튼. 나머지는 정상 동작 |
| **E-05** | 검색 결과 0건 | 드롭다운 내 빈 상태 | 안내 문구 + 추천 도시 3개 |
| **E-06** | 필터 결과 0건 | 메인 영역 빈 상태 | `필터 초기화` |
| **E-07** | 잘못된 URL 파라미터 (없는 spot ID 등) | 기본 상태로 폴백 | 토스트 `해당 장소를 찾을 수 없어 기본 화면으로 이동했어요` |
| **E-08** | 지도 렌더 실패 | 리스트 뷰로 자동 폴백 | 토스트 안내 |

**에러 UI 공통 원칙**

1. 원인 → 영향 → 다음 행동 순으로 문장을 구성한다. HTTP 상태 코드를 사용자에게 그대로 노출하지 않는다.
2. **부분 실패는 전체 실패로 처리하지 않는다.** 26개 중 3개가 실패하면 23개는 정상 표시한다.
3. 마지막 성공 응답을 캐시로 보관하고, 갱신 실패 시 `n분 전 기준` 라벨과 함께 캐시 데이터를 계속 보여준다.
4. 재시도는 지수 백오프(1s → 2s → 4s, 최대 3회) 후 사용자 액션으로 전환한다.

### F-08. 푸터 / 출처 표기 (P0 — 법적 요구사항)

- Open-Meteo 데이터는 **CC BY 4.0** 라이선스이며 **출처 표기가 의무**다.
- 푸터에 `Weather data by Open-Meteo.com (CC BY 4.0)` 링크를 상시 노출한다.

---

## 7. 데이터 스펙 — Open-Meteo 연동

### 7.1 Forecast API

```
GET https://api.open-meteo.com/v1/forecast
```

| 파라미터 | 값 |
|---|---|
| `latitude` / `longitude` | 랜드마크 좌표 (다중 좌표는 콤마로 묶어 한 번에 요청 가능) |
| `current` | `temperature_2m,relative_humidity_2m,apparent_temperature,is_day,precipitation,weather_code,cloud_cover,wind_speed_10m` |
| `daily` | `weather_code,temperature_2m_max,temperature_2m_min,sunrise,sunset` |
| `timezone` | `auto` (좌표 기준 현지 시간대 자동 적용) |
| `forecast_days` | `3` |
| `wind_speed_unit` | `ms` |

**주요 응답 필드**

```jsonc
{
  "utc_offset_seconds": 7200,        // 현지 시간 계산의 기준값
  "timezone": "Europe/Paris",
  "timezone_abbreviation": "CEST",
  "current": {
    "time": "2026-08-18T14:00",
    "temperature_2m": 24.3,
    "apparent_temperature": 25.1,
    "relative_humidity_2m": 58,
    "is_day": 1,                     // 1=낮, 0=밤 → 필터 & 배경 폴백
    "precipitation": 0.0,
    "weather_code": 3,               // WMO 코드 → FX 그룹 매핑
    "cloud_cover": 82,
    "wind_speed_10m": 3.4
  },
  "current_units": { /* 단위 문자열. UI 표기에 그대로 사용 */ },
  "daily": {
    "time": ["2026-08-18", ...],
    "weather_code": [3, ...],
    "temperature_2m_max": [26.1, ...],
    "temperature_2m_min": [17.4, ...],
    "sunrise": ["2026-08-18T06:52", ...],
    "sunset":  ["2026-08-18T21:03", ...]
  }
}
```

> ⚠️ **다중 좌표 요청 시 응답은 위 객체의 배열(`[{...}, {...}]`)로 반환된다.** 단일 좌표일 때는 객체 하나다.
> 파서는 두 형태를 모두 처리해야 한다. 배열 순서는 요청한 좌표 순서와 동일하다.
>
> ⚠️ 필드명·응답 형태는 구현 착수 시점에 실제 응답으로 재검증할 것. (본 문서 작성 환경에서는 외부 API 직접 호출이 차단되어 공식 문서 기준으로 작성됨)

### 7.2 Geocoding API (검색용)

```
GET https://geocoding-api.open-meteo.com/v1/search?name={query}&count=8&language=ko
```

- 응답: `results[]` — `id, name, latitude, longitude, country, country_code, admin1, timezone, population, feature_code, elevation`
- **결과 없음**: `results` 키 자체가 없을 수 있음 → `results?.length` 로 방어 처리
- **에러**: `{ "error": true, "reason": "..." }`
- 2글자 미만 쿼리는 결과 없음 → 클라이언트에서 사전 차단
- `population` 내림차순 정렬로 주요 도시를 상단 노출

### 7.3 호출량 & 캐싱 전략

**무료 티어 제한 (Non-Commercial)**

| 단위 | 제한 |
|---|---|
| 분 | 600 calls |
| 시간 | 5,000 calls |
| 일 | 10,000 calls |
| 월 | 300,000 calls |

> 10개를 초과하는 변수를 요청하면 1건이 1건 이상으로 계산된다(소수점 과금).
> 현재 설계는 `current` 8개 + `daily` 5개 = 13개 변수 → **약 1.3 calls/요청**으로 추정.

**대응 전략 (필수)**

1. **다중 좌표 일괄 요청**: `latitude=48.85,41.89,...` 형태로 26개 랜드마크를 **1회 요청**으로 처리한다. → 초기 로딩 API 호출 1건.
2. **서버 사이드 프록시 + 캐시**: 클라이언트가 Open-Meteo를 직접 호출하지 않고 자체 API 라우트를 경유한다.
   - 랜드마크 전체 날씨는 서버에서 **10분 TTL 캐시**. 모든 사용자가 같은 캐시를 공유 → 트래픽과 무관하게 **시간당 6회 호출**로 고정.
   - 이 구조 없이는 동시 사용자 증가 시 무료 티어를 즉시 초과한다. **P0**
3. 검색(Geocoding)은 사용자별 쿼리라 캐시 효율이 낮으므로 디바운스 300ms + 동일 쿼리 클라이언트 캐시로 방어한다.
4. 클라이언트는 10분 주기로 캐시를 재조회한다. 탭이 백그라운드일 때는 폴링을 정지한다.

---

## 8. 랜드마크 데이터셋 (26개)

각 항목은 `id`, `nameKo`, `nameEn`, `city`, `country`, `countryCode`, `region`, `lat`, `lon`, `timezone`, `imageUrl` 필드를 갖는다.

| # | 랜드마크 | 도시 · 국가 | 지역 | 위도 | 경도 |
|---|---|---|---|---|---|
| 1 | 경복궁 | 서울 · 대한민국 | 아시아 | 37.5796 | 126.9770 |
| 2 | 도쿄 타워 | 도쿄 · 일본 | 아시아 | 35.6586 | 139.7454 |
| 3 | 만리장성 (팔달령) | 베이징 · 중국 | 아시아 | 40.4319 | 116.5704 |
| 4 | 타지마할 | 아그라 · 인도 | 아시아 | 27.1751 | 78.0421 |
| 5 | 마리나 베이 샌즈 | 싱가포르 | 아시아 | 1.2834 | 103.8607 |
| 6 | 부르즈 할리파 | 두바이 · UAE | 아시아 | 25.1972 | 55.2744 |
| 7 | 앙코르 와트 | 시엠레아프 · 캄보디아 | 아시아 | 13.4125 | 103.8670 |
| 8 | 에펠탑 | 파리 · 프랑스 | 유럽 | 48.8584 | 2.2945 |
| 9 | 콜로세움 | 로마 · 이탈리아 | 유럽 | 41.8902 | 12.4922 |
| 10 | 사그라다 파밀리아 | 바르셀로나 · 스페인 | 유럽 | 41.4036 | 2.1744 |
| 11 | 타워 브리지 | 런던 · 영국 | 유럽 | 51.5055 | −0.0754 |
| 12 | 이아 마을 | 산토리니 · 그리스 | 유럽 | 36.4618 | 25.3753 |
| 13 | 트롬쇠 | 트롬쇠 · 노르웨이 | 유럽 | 69.6492 | 18.9553 |
| 14 | 하기아 소피아 | 이스탄불 · 튀르키예 | 유럽 | 41.0086 | 28.9802 |
| 15 | 자유의 여신상 | 뉴욕 · 미국 | 북미 | 40.6892 | −74.0445 |
| 16 | 그랜드 캐니언 | 애리조나 · 미국 | 북미 | 36.1069 | −112.1129 |
| 17 | 금문교 | 샌프란시스코 · 미국 | 북미 | 37.8199 | −122.4783 |
| 18 | 치첸이트사 | 유카탄 · 멕시코 | 북미 | 20.6843 | −88.5678 |
| 19 | 마추픽추 | 쿠스코 · 페루 | 남미 | −13.1631 | −72.5450 |
| 20 | 그리스도상 | 리우데자네이루 · 브라질 | 남미 | −22.9519 | −43.2105 |
| 21 | 이구아수 폭포 | 미시오네스 · 아르헨티나 | 남미 | −25.6953 | −54.4367 |
| 22 | 기자 피라미드 | 기자 · 이집트 | 아프리카 | 29.9792 | 31.1342 |
| 23 | 빅토리아 폭포 | 리빙스턴 · 잠비아 | 아프리카 | −17.9243 | 25.8572 |
| 24 | 테이블 마운틴 | 케이프타운 · 남아프리카공화국 | 아프리카 | −33.9628 | 18.4098 |
| 25 | 시드니 오페라 하우스 | 시드니 · 호주 | 오세아니아 | −33.8568 | 151.2153 |
| 26 | 밀포드 사운드 | 피오르드랜드 · 뉴질랜드 | 오세아니아 | −44.6414 | 167.8974 |

**선정 기준**

- 6개 대륙 고르게 분포 (아시아 7 / 유럽 7 / 북미 4 / 남미 3 / 아프리카 3 / 오세아니아 2)
- 경도를 넓게 분산 → 어느 시각에 접속해도 낮·밤이 동시에 존재
- 극한 기후 대비 지점 포함 (트롬쇠 ↔ 두바이, 밀포드 사운드 ↔ 기자)
- 좌표는 **랜드마크 실제 위치** 기준 (도시 중심이 아님)

> 좌표·시간대는 구현 전 실제 API 응답의 `timezone` 값과 대조 검증할 것.

---

## 9. 비기능 요구사항

### 9.1 성능

| 항목 | 기준 |
|---|---|
| LCP | 2.5초 이하 (모바일 4G) |
| INP | 200ms 이하 |
| 배경 전환 프레임 | 55fps 이상 유지 |
| 파티클 개수 상한 | 동시 300개 (저사양 기기 감지 시 100개로 하향) |
| 초기 번들 | 300KB (gzip) 이하 |

### 9.2 반응형

→ **§10. 반응형 설계** 참조 (분량이 커 독립 챕터로 분리)

### 9.3 접근성 (WCAG 2.1 AA)

- 모든 인터랙티브 요소 키보드 접근 가능. 지도 핀은 Tab 순회 + Enter 선택 지원.
- 배경 그라디언트 위 텍스트는 대비 4.5:1 이상 확보 (필요 시 반투명 backdrop 레이어 삽입).
- 날씨 정보는 아이콘만이 아니라 **텍스트로도** 제공 (색·아이콘 단독 의존 금지).
- `prefers-reduced-motion` 필수 대응 (§4.3).
- 상세 패널은 포커스 트랩 적용, 닫으면 트리거 요소로 포커스 복귀.

### 9.4 브라우저 지원

Chrome / Edge / Safari / Firefox 최신 2개 버전, iOS Safari 16+, Android Chrome 최신.

---

## 10. 반응형 설계

**설계 원칙**

1. **모바일 퍼스트.** 기본 스타일은 모바일 기준으로 작성하고 `min-width` 미디어쿼리로 확장한다.
2. **뷰포트 폭이 아니라 입력 방식으로 분기한다.** 화면이 넓어도 터치 기기일 수 있다 (iPad, 터치 노트북). 레이아웃은 폭으로, 인터랙션은 `pointer`/`hover` 미디어 쿼리로 분기한다.
3. **뷰는 전환되어도 상태는 유지된다.** 브레이크포인트를 넘나들며 리사이즈해도 선택된 랜드마크·필터·스크롤 위치가 초기화되지 않는다.
4. **어떤 화면에서도 5개 필수 요소(로딩·에러·검색·필터·리스트)는 도달 가능해야 한다.** 좁은 화면에서 기능을 *제거*하지 않고 *접는다*.

### 10.1 브레이크포인트 정의

| 토큰 | 범위 | 대표 기기 | 비고 |
|---|---|---|---|
| `xs` | ~479px | iPhone SE, 소형 안드로이드 | 최소 지원 폭 **320px** |
| `sm` | 480~767px | 대형 스마트폰, 폴더블 접힘 | |
| `md` | 768~1023px | 태블릿 세로, 폴더블 펼침 | |
| `lg` | 1024~1439px | 태블릿 가로, 노트북 | 데스크톱 레이아웃 진입점 |
| `xl` | 1440px~ | 데스크톱 모니터 | 콘텐츠 최대 폭 1600px로 제한 |

```css
/* 모바일 퍼스트 — min-width만 사용 */
:root { --bp-sm: 480px; --bp-md: 768px; --bp-lg: 1024px; --bp-xl: 1440px; }
```

**입력 방식 분기 (레이아웃과 독립)**

```css
@media (hover: hover) and (pointer: fine)  { /* 마우스: hover 툴팁 허용 */ }
@media (hover: none)  or  (pointer: coarse) { /* 터치: hover 대체 UI 필수 */ }
```

### 10.2 레이아웃 매트릭스

| 컴포넌트 | `xs` / `sm` (모바일) | `md` (태블릿) | `lg` / `xl` (데스크톱) |
|---|---|---|---|
| **상단 바** | 로고 축약(심볼만) + 검색 아이콘 + 뷰 토글 | 로고 + 검색창(축소) + 토글 | 로고 + 검색창(360px) + 토글 |
| **검색창** | 아이콘 탭 → **전체화면 오버레이** | 인라인 확장 (탭 시 폭 확대) | 상시 인라인 노출 |
| **필터** | 하단 고정 `필터` 버튼 → **바텀시트 모달** | 상단 접이식 바 (기본 접힘) | 상단 상시 노출 바 |
| **지도** | 전체 화면 (edge-to-edge) | 전체 폭, 하단 240px 드로어 여백 | 좌측 가변 영역 (패널 제외 전체) |
| **상세 패널** | **바텀시트** (3단 스냅) | 하단 드로어 (드래그로 높이 조절) | 우측 고정 패널 **400px** (`xl`은 440px) |
| **리스트 뷰** | 1열 | 2열 | 3열 (`xl`은 4열) |
| **3일 예보** | 가로 스크롤 카드 | 3열 그리드 | 3열 그리드 |
| **배경 레이어** | 전체 화면 고정 | 전체 화면 고정 | 전체 화면 고정 |
| **푸터/출처** | 필터 시트 내부로 이동 | 하단 고정 (12px) | 하단 좌측 고정 |

### 10.3 컴포넌트별 상세 동작

#### 10.3.1 지도 (F-01)

| 항목 | 모바일 | 태블릿 | 데스크톱 |
|---|---|---|---|
| 기본 줌 | 1.5 (전 지구가 들어오도록) | 2 | 2.5 |
| 핀 시각 크기 | 28px | 32px | 32px |
| **핀 히트 영역** | **44×44px** (시각 크기와 무관, 투명 확장) | 44×44px | 32×32px |
| 핀 라벨 | 기온만 (랜드마크명 생략) | 기온 + 축약명 | 기온 + 전체명 |
| 클러스터링 임계 | 핀 간격 48px 미만 | 40px 미만 | 32px 미만 |
| 줌 컨트롤 | 우측 하단 +/− 버튼 **필수** (핀치만으로 부족) | 우측 하단 | 우측 상단 |

- **R-MAP-1** 상세 패널이 열리면 지도의 **가시 중심(visual center)** 을 보정한다. 데스크톱은 패널 폭 400px만큼 좌측으로, 모바일은 바텀시트 높이만큼 위로 선택 핀을 이동시켜 핀이 가려지지 않게 한다.
- **R-MAP-2** 모바일 세로에서 지도는 `100dvh`를 채우되, 상단 바(56px)와 하단 필터 버튼(안전영역 포함)은 지도 위에 **오버레이**한다. 지도 영역을 잘라내지 않는다.
- **R-MAP-3** 리사이즈 시 지도는 현재 중심 좌표·줌을 유지한 채 뷰포트만 재계산한다. `ResizeObserver` + 150ms 디바운스.

#### 10.3.2 상세 패널 (F-02) — 바텀시트 스펙

모바일의 핵심 컴포넌트이므로 별도 정의한다.

| 스냅 포인트 | 높이 | 노출 내용 |
|---|---|---|
| `peek` (기본) | `36dvh` | 랜드마크명, 현지 시간, 현재 기온, 날씨 아이콘·상태 |
| `half` | `62dvh` | + 체감온도, 습도, 강수량, 풍속, 구름량, 일출·일몰 |
| `full` | `92dvh` | + 3일 예보, 액션 버튼 |

- **R-SHEET-1** 핀 탭 시 `peek`으로 열린다. 위로 드래그하면 `half` → `full`, 아래로 드래그하면 닫힌다.
- **R-SHEET-2** 스냅 판정은 **드래그 속도(velocity)** 우선, 정지 시 최근접 스냅으로 흡착한다. 애니메이션 `280ms cubic-bezier(0.32, 0.72, 0, 1)`.
- **R-SHEET-3** **제스처 충돌 해소 규칙** (필수)
  - 시트 내부 스크롤이 최상단(`scrollTop === 0`)일 때만 아래 방향 드래그가 시트 이동으로 전달된다.
  - 그 외에는 내부 스크롤이 우선한다.
  - 시트가 열려 있는 동안 배경 지도의 팬/줌 제스처는 차단한다. (`touch-action: none` on backdrop)
  - `overscroll-behavior: contain`으로 body 스크롤 체이닝을 막는다.
- **R-SHEET-4** 상단에 **드래그 핸들**(폭 36px, 높이 4px)을 표시해 드래그 가능함을 알린다. 핸들 자체의 히트 영역은 44px 이상.
- **R-SHEET-5** 하단 패딩에 `env(safe-area-inset-bottom)`을 더해 홈 인디케이터와 겹치지 않게 한다.
- **R-SHEET-6** `peek` 상태에서도 배경(§4)은 이미 전환이 완료되어 있어야 한다. 시트를 올려야 배경이 바뀌는 구조는 금지.
- **R-SHEET-7** 데스크톱 우측 패널은 스냅 개념 없이 전체 내용을 세로 스크롤로 노출한다. 패널 높이는 `calc(100dvh - 상단바 - 32px)`.

#### 10.3.3 검색창 (F-03)

- **R-SEARCH-1** 모바일(`xs`/`sm`)에서는 아이콘 탭 시 **전체화면 검색 오버레이**로 전환한다. 좁은 폭에서 드롭다운은 결과를 2~3개밖에 못 보여준다.
  - 오버레이: 상단 입력창 + `취소` 버튼, 하단 결과 리스트(전체 높이 사용)
  - 진입 시 자동 포커스 + 키보드 호출
  - 결과 항목 높이 **56px 이상** (터치 타겟 확보)
- **R-SEARCH-2** **모바일 키보드가 올라온 상태**에서 결과 리스트가 가려지지 않도록 `visualViewport` API로 실제 가시 높이를 계산해 리스트 높이를 조정한다. `100vh` 사용 금지.
- **R-SEARCH-3** iOS 자동 확대 방지를 위해 입력 필드 `font-size`는 **16px 이상**으로 고정한다.
- **R-SEARCH-4** 태블릿 이상에서는 드롭다운 방식. 최대 높이 `min(400px, 50dvh)`, 초과 시 내부 스크롤.
- **R-SEARCH-5** 검색 오버레이/드롭다운 활성 중 `ESC`(데스크톱) 또는 `취소`(모바일)로 닫히며, 닫으면 직전 포커스로 복귀한다.

#### 10.3.4 필터 (F-04)

- **R-FILTER-1** 모바일에서는 하단 고정 `필터 (n)` 버튼 → **바텀시트 모달**. `적용` 버튼을 눌러야 반영되며, `초기화`는 좌측 상단에 배치한다. (즉시 반영 시 화면 뒤에서 결과가 바뀌는 것을 볼 수 없어 혼란)
- **R-FILTER-2** 태블릿 이상은 **즉시 반영**. `적용` 버튼 없음.
- **R-FILTER-3** 대륙·날씨 칩은 모바일에서 **가로 스크롤 단일 행**이 아니라 **줄바꿈(wrap) 그리드**로 배치한다. 가로 스크롤은 뒤쪽 옵션의 발견율을 떨어뜨린다.
- **R-FILTER-4** 기온 슬라이더의 핸들 히트 영역은 터치 기기에서 **44×44px**로 확장한다. 현재 값은 핸들 위에 상시 라벨로 표시한다 (드래그 중 손가락에 가려지므로).
- **R-FILTER-5** 적용된 필터 개수 뱃지는 모든 브레이크포인트에서 노출한다. 모바일에서 시트가 닫혀 있어도 필터 상태를 인지할 수 있어야 한다.

#### 10.3.5 리스트 뷰 (F-05)

```css
.landmark-grid {
  display: grid;
  gap: 12px;
  grid-template-columns: 1fr;                     /* xs, sm */
}
@media (min-width: 768px)  { .landmark-grid { gap: 16px; grid-template-columns: repeat(2, 1fr); } }
@media (min-width: 1024px) { .landmark-grid { gap: 20px; grid-template-columns: repeat(3, 1fr); } }
@media (min-width: 1440px) { .landmark-grid { grid-template-columns: repeat(4, 1fr); } }
```

- **R-LIST-1** 카드 내부 레이아웃은 **컨테이너 쿼리**로 제어한다. 같은 카드가 1열(넓음)과 3열(좁음)에 모두 놓이므로 뷰포트 기준 분기는 어긋난다.
  ```css
  .landmark-card { container-type: inline-size; }
  @container (min-width: 340px) { /* 가로 배치: 아이콘 좌 · 정보 우 */ }
  @container (max-width: 339px) { /* 세로 배치 */ }
  ```
- **R-LIST-2** 정렬 컨트롤은 모바일에서 드롭다운(`<select>` 네이티브), 태블릿 이상에서 세그먼트 컨트롤.
- **R-LIST-3** 랜드마크가 26개로 고정이므로 가상 스크롤은 불필요하다. 향후 100개 확장 시 재검토. (백로그)
- **R-LIST-4** 카드 전체가 클릭 영역이며 최소 높이 **88px**를 보장한다.

### 10.4 터치 & 포인터 인터랙션

| 항목 | 규칙 |
|---|---|
| 최소 터치 타겟 | **44×44px** (WCAG 2.5.5 / Apple HIG). 시각 크기가 작아도 투명 영역으로 확장 |
| 인접 타겟 간격 | 최소 8px |
| hover 의존 금지 | 지도 핀 툴팁(F-01-3), 카드 hover 효과 등은 터치 기기에서 **탭 = 선택**으로 대체. hover에만 존재하는 정보는 만들지 않는다 |
| `:hover` 스타일 격리 | `@media (hover: hover)` 안에만 작성. 그러지 않으면 터치 후 hover 상태가 고착된다 |
| 탭 하이라이트 | `-webkit-tap-highlight-color: transparent` + 자체 `:active` 상태 정의 |
| 더블탭 확대 방지 | 인터랙티브 요소에 `touch-action: manipulation` |
| 스와이프 예약 | 좌우 스와이프는 브라우저 뒤로가기와 충돌하므로 **화면 가장자리 20px 영역에는 자체 스와이프를 할당하지 않는다** |

### 10.5 타이포그래피 & 스페이싱 스케일

브레이크포인트마다 폰트 크기를 재정의하지 않고 `clamp()`로 연속 스케일링한다.

```css
:root {
  /* 320px ~ 1440px 구간에서 선형 보간 */
  --fs-caption: clamp(11px, 0.5vw + 9.4px,  13px);
  --fs-body:    clamp(14px, 0.5vw + 12.4px, 16px);
  --fs-title:   clamp(18px, 1.1vw + 14.5px, 24px);
  --fs-temp-lg: clamp(48px, 5.4vw + 30.7px, 108px);  /* 상세 패널 대형 기온 */
  --fs-clock:   clamp(28px, 2.5vw + 20px,   56px);   /* 현지 시계 */

  --sp-1: 4px;  --sp-2: 8px;  --sp-3: 12px;
  --sp-4: 16px; --sp-6: 24px; --sp-8: 32px;
  --gutter: clamp(16px, 4vw, 40px);   /* 화면 좌우 여백 */
}
```

- **R-TYPE-1** 현지 시계(`--fs-clock`)는 `font-variant-numeric: tabular-nums`를 적용해 초가 바뀔 때 폭이 흔들리지 않게 한다. 이게 없으면 1초마다 레이아웃이 떨린다.
- **R-TYPE-2** 사용자가 브라우저 기본 폰트 크기를 키운 경우를 위해 `rem` 기반으로 환산 가능해야 한다. `px` 하드코딩은 위 토큰 정의부에만 허용.
- **R-TYPE-3** 랜드마크명이 긴 경우(`시드니 오페라 하우스`) 모바일에서 2줄까지 허용하고 그 이상은 말줄임 처리한다.

### 10.6 뷰포트 · 세이프에어리어

- **R-VP-1** `100vh` **사용 금지**. iOS Safari의 주소창 때문에 실제 가시 영역보다 커진다. `100dvh`를 사용하고, 미지원 브라우저를 위해 fallback을 둔다.
  ```css
  .full-screen { height: 100vh; height: 100dvh; }
  ```
- **R-VP-2** 노치·홈 인디케이터 대응 — 배경은 화면 끝까지 채우되 콘텐츠는 안전영역 안에 둔다.
  ```html
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  ```
  ```css
  .app-bar    { padding-top:    max(var(--sp-3), env(safe-area-inset-top)); }
  .bottom-bar { padding-bottom: max(var(--sp-3), env(safe-area-inset-bottom)); }
  .sheet      { padding-inline: max(var(--sp-4), env(safe-area-inset-left)); }
  ```
- **R-VP-3** 배경 그라디언트·파티클 캔버스는 **안전영역을 무시하고 전체 화면**을 덮는다. 노치 주변이 비면 몰입이 깨진다.
- **R-VP-4** 사용자 확대(pinch zoom)를 막지 않는다. `user-scalable=no` / `maximum-scale=1` **금지** (WCAG 1.4.4 위반).
- **R-VP-5** 최소 지원 폭 **320px**에서 가로 스크롤이 발생하지 않아야 한다. QA 필수 항목.

### 10.7 가로 모드 (Landscape)

모바일 가로는 세로 높이가 380px 내외로 극도로 짧아 바텀시트가 사실상 동작하지 않는다.

- **R-LAND-1** `(max-width: 1023px) and (orientation: landscape) and (max-height: 500px)` 조건에서 상세 패널을 **바텀시트 대신 좌측 사이드 패널(폭 320px)** 로 전환한다.
- **R-LAND-2** 같은 조건에서 상단 바 높이를 56px → 44px로 축소하고, 필터는 바텀시트 대신 좌측 슬라이드 패널로 노출한다.
- **R-LAND-3** 태블릿 가로(`md` 이상, height 500px 초과)는 데스크톱 레이아웃 규칙을 그대로 따른다.

### 10.8 기기 성능 분기

배경 파티클(§4.2)은 반응형 설계의 성능 리스크 지점이다. 화면 크기가 아니라 **기기 성능**으로 분기한다.

| 조건 | 파티클 상한 | 추가 처리 |
|---|---|---|
| `navigator.hardwareConcurrency >= 8` && `deviceMemory >= 8` | 300개 | 전체 효과 |
| 그 외 / 값 미제공 | 100개 | 블러 반경 축소, 번개 빈도 하향 |
| `prefers-reduced-motion: reduce` | **0개** | 정적 그라디언트만 |
| 탭 비활성 (`visibilitychange`) | 렌더 루프 정지 | |
| 저전력 모드 감지 시 (가능한 경우) | 100개 | |

- **R-PERF-1** Canvas 해상도는 `devicePixelRatio`를 곱하되 **최대 2배로 제한**한다. DPR 3인 기기에서 3배 렌더는 프레임을 떨어뜨린다.
- **R-PERF-2** 배경 그라디언트 전환은 `background` 애니메이션 대신 **두 레이어의 `opacity` 크로스페이드**로 구현한다 (GPU 합성, 리페인트 회피).
- **R-PERF-3** 실측 프레임이 3초 연속 40fps 미만이면 파티클 상한을 자동으로 한 단계 낮춘다.

### 10.9 반응형 QA 체크리스트

| # | 항목 | 기준 |
|---|---|---|
| Q1 | 320px 폭에서 가로 스크롤 없음 | 필수 |
| Q2 | iOS Safari 주소창 노출/숨김 시 레이아웃 깨짐 없음 | 필수 |
| Q3 | 모바일 키보드 활성 시 검색 결과가 가려지지 않음 | 필수 |
| Q4 | 바텀시트 내부 스크롤과 시트 드래그가 충돌하지 않음 | 필수 |
| Q5 | 노치·홈 인디케이터에 콘텐츠가 가리지 않음 | 필수 |
| Q6 | 모든 터치 타겟 44×44px 이상 | 필수 |
| Q7 | 터치 후 hover 상태 고착 없음 | 필수 |
| Q8 | 모바일 가로 모드에서 상세 정보 도달 가능 | 필수 |
| Q9 | 브레이크포인트 경계(767/768, 1023/1024)에서 리사이즈 시 상태 유지 | 필수 |
| Q10 | 현지 시계 초 갱신 시 레이아웃 흔들림 없음 | 필수 |
| Q11 | 폴더블 접힘/펼침 전환 시 레이아웃 복구 | 권장 |
| Q12 | 브라우저 폰트 크기 200%에서 콘텐츠 손실 없음 | 권장 |

**테스트 기기 매트릭스 (최소)**

| 구분 | 기기 | 폭 |
|---|---|---|
| 최소 | iPhone SE (3rd) | 375px |
| 표준 | iPhone 15 / Galaxy S23 | 390~393px |
| 대형 | iPhone 15 Pro Max | 430px |
| 태블릿 세로 | iPad (10.9") | 820px |
| 태블릿 가로 | iPad (10.9") | 1180px |
| 노트북 | MacBook Air | 1440px |
| 데스크톱 | 27" 모니터 | 1920px |

---

## 11. 성공 지표

| 구분 | 지표 | 목표 (출시 1개월) |
|---|---|---|
| 참여 | 세션당 평균 랜드마크 조회 수 | 3개 이상 |
| 참여 | 평균 세션 길이 | 90초 이상 |
| 참여 | 검색 또는 필터 사용 세션 비율 | 30% 이상 |
| 성장 | URL 공유를 통한 유입 비율 | 전체 유입의 10% 이상 |
| 품질 | API 오류로 인한 세션 이탈률 | 3% 이하 |
| 품질 | LCP 2.5초 이하 달성률 | 75% 이상 |
| 재방문 | 7일 재방문율 | 15% 이상 |

**트래킹 이벤트 (최소 정의)**
`app_loaded`, `landmark_selected(id, source: map|list|search)`, `search_performed(query, result_count)`, `filter_applied(type, value)`, `view_toggled(to)`, `link_copied(spot)`, `error_shown(code)`

---

## 12. 릴리즈 범위

### MVP (v1.0)

- [x] 인터랙티브 세계지도 + 26개 랜드마크 핀 (F-01)
- [x] 랜드마크 상세 패널 + 실시간 현지 시계 (F-02)
- [x] 2레이어 배경 전환 시스템 (§4)
- [x] 도시 검색창 + 자동완성 (F-03)
- [x] 4종 필터 (F-04)
- [x] 리스트 뷰 + 정렬 (F-05)
- [x] 3단계 로딩 UI (F-06)
- [x] 8종 에러 처리 (F-07)
- [x] 서버 프록시 + 10분 캐시 (§7.3)
- [x] URL 상태 공유
- [x] 반응형 5단계 브레이크포인트 + 바텀시트 상세 패널 (§10)
- [x] 모바일 가로 모드 대응 (§10.7)
- [x] 기기 성능 기반 파티클 분기 (§10.8)
- [x] 접근성 WCAG 2.1 AA (§9.3)

### v1.1

- 즐겨찾기 (localStorage)
- 영문 지원 (i18n)
- 3일 → 7일 예보 확장
- 랜드마크 실사 이미지 레이어 (배경 그라디언트와 블렌딩)
- OG 이미지 동적 생성 (공유 시 해당 랜드마크의 현재 하늘이 썸네일로)

### 백로그

- 랜드마크 확장 (50~100개) + 사용자 제안 접수
- 시간 슬라이더 (24시간 예보를 스크럽하며 배경 변화 감상)
- 사운드 레이어 (빗소리, 바람소리 — 음소거 기본)
- 회원 기능 및 서버 동기화

---

## 13. 오픈 이슈

| ID | 이슈 | 결정 필요 시점 |
|---|---|---|
| **OI-1** | 지도 구현 방식: d3-geo SVG 자체 렌더 vs Leaflet+타일 vs MapLibre GL | 설계 착수 전 |
| **OI-2** | 배경 애셋 제작 방식: CSS 그라디언트+Canvas 파티클(코드) vs 디자이너 제작 이미지 | 디자인 킥오프 |
| **OI-3** | 서버 프록시 호스팅 (Vercel Edge Function / Cloudflare Workers) 및 캐시 스토어 선정 | 개발 착수 전 |
| **OI-4** | 검색으로 찾은 임의 도시는 필터/리스트 대상에 포함시킬 것인가 (현재 안: 상세 패널만 열고 목록에는 미포함) | 기획 확정 |
| **OI-5** | Open-Meteo 상업적 이용 판단 — 서비스에 광고/수익모델이 붙는 경우 유료 플랜 필요 | 수익화 논의 시 |
| **OI-6** | 랜드마크 이미지 저작권 처리 (v1.1 실사 이미지 도입 시) | v1.1 기획 시 |
| **OI-7** | 바텀시트를 직접 구현할 것인가, 라이브러리(Vaul 등)를 쓸 것인가 — 제스처 충돌 처리(R-SHEET-3)가 자체 구현 시 가장 큰 리스크 | 설계 착수 전 |
| **OI-8** | 컨테이너 쿼리(R-LIST-1) 사용 시 지원 브라우저 하한 확정 필요 (Safari 16+ 필요) | 설계 착수 전 |

---

## 부록 A. WMO Weather Code 전체 매핑표

| 코드 | 의미 | 한국어 표기 | FX 그룹 |
|---|---|---|---|
| 0 | Clear sky | 맑음 | `clear` |
| 1 | Mainly clear | 대체로 맑음 | `clear` |
| 2 | Partly cloudy | 구름 조금 | `cloudy` |
| 3 | Overcast | 흐림 | `cloudy` |
| 45 | Fog | 안개 | `fog` |
| 48 | Depositing rime fog | 서리 안개 | `fog` |
| 51 | Drizzle: light | 약한 이슬비 | `rain` (1) |
| 53 | Drizzle: moderate | 이슬비 | `rain` (1) |
| 55 | Drizzle: dense | 강한 이슬비 | `rain` (2) |
| 56 | Freezing drizzle: light | 약한 어는 이슬비 | `rain` (1) |
| 57 | Freezing drizzle: dense | 강한 어는 이슬비 | `rain` (2) |
| 61 | Rain: slight | 약한 비 | `rain` (1) |
| 63 | Rain: moderate | 비 | `rain` (2) |
| 65 | Rain: heavy | 강한 비 | `rain` (3) |
| 66 | Freezing rain: light | 약한 어는 비 | `rain` (2) |
| 67 | Freezing rain: heavy | 강한 어는 비 | `rain` (3) |
| 71 | Snow fall: slight | 약한 눈 | `snow` (1) |
| 73 | Snow fall: moderate | 눈 | `snow` (2) |
| 75 | Snow fall: heavy | 강한 눈 | `snow` (3) |
| 77 | Snow grains | 싸락눈 | `snow` (1) |
| 80 | Rain showers: slight | 약한 소나기 | `rain` (2) |
| 81 | Rain showers: moderate | 소나기 | `rain` (2) |
| 82 | Rain showers: violent | 강한 소나기 | `rain` (3) |
| 85 | Snow showers: slight | 약한 소낙눈 | `snow` (2) |
| 86 | Snow showers: heavy | 강한 소낙눈 | `snow` (3) |
| 95 | Thunderstorm | 뇌우 | `thunder` |
| 96 | Thunderstorm with slight hail | 우박 동반 뇌우 | `thunder` |
| 99 | Thunderstorm with heavy hail | 강한 우박 동반 뇌우 | `thunder` |

*괄호 안 숫자는 파티클 밀도 단계 (1: 약 / 2: 중 / 3: 강)*

---

## 부록 B. 데이터 출처

- Weather data by [Open-Meteo.com](https://open-meteo.com) — CC BY 4.0 (출처 표기 의무)
- Geocoding: Open-Meteo Geocoding API (GeoNames 기반)
